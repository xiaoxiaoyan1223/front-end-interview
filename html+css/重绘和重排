重绘：当元素的样式发生改变，但是不影响布局时，浏览器会重新绘制这些元素的过程。重绘不影响页面的布局，只更新元素的视觉效果
重排：当页面布局发生改变时，需要重新计算元素的位置和大小，浏览器会触发重排，重排会导致部分页面或整个页面重新布局。
减少重排：①使用css动画代替JavaScript操作dom，浏览器可以优化css动画性能
         ②避免频繁操作样式，最好一次性修改完，或者将修改操作集中到一起执行
         ③离线操作dom，现将dom元素从文档中移除，完成操作后再将其添加会文档
